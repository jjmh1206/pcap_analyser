# pcap_analyser
Python script for automatic .pcap analysis.

Use the file pcap_analyser.pcap for program execution. To make sure the program is fully functional, make sure that all associated files reside within the same directory as pcap_analyser.pcap.
